{
  "name": "swerve",
  "version": "0.1.0",
  "description": "Serverless webRTC version.",
  "main": "index.js",
  "scripts": {
    "build": "npx browserify index.js -o main.js && uglifyjs main.js -c -o index.min.js --verbose --warn"
  },
  "keywords": [
    "html",
    "javascript",
    "node",
    "webRTC",
    "security",
    "p2p"
  ],
  "author": "fr33roam3r",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "browserify": "^17.0.0",
    "uglify-js": "^3.12.1"
  },
  "dependencies": {
    "webrtc-adapter": "^7.7.0"
  }
}
